{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build && cp .nojekyll build/",
    "push": "./deploy.sh",
    "publish": "npm run build && npm run push",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "devDependencies": {
    "@snowpack/plugin-svelte": "^3.7.0",
    "snowpack": "^3.8.8"
  },
  "dependencies": {
    "svelte": "^3.42.5"
  }
}
